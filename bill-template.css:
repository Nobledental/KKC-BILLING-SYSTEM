/* ============================================================
   Premium Modern Medical Invoice (Teal + Dark Blue)
============================================================ */

.bill-wrapper {
  width: 100%;
  padding: 40px;
  font-family: "Manrope", sans-serif;
  color: #0b1a33;
}

.bill-header {
  display: flex;
  justify-content: space-between;
  margin-bottom: 10px;
}

.bill-logo-large {
  width: 180px;
  height: auto;
}

.bill-hospital-name {
  font-size: 30px;
  font-weight: 800;
  margin-bottom: 8px;
  color: #003e8a;
}

.bill-hospital-details {
  font-size: 14px;
  line-height: 1.45;
  color: #445069;
}

.bill-header-gradient {
  height: 5px;
  width: 100%;
  margin: 20px 0 30px;
  background: linear-gradient(to right, #1ba7a5, #003e8a);
  border-radius: 3px;
}

.bill-section h2 {
  font-size: 18px;
  font-weight: 700;
  color: #003e8a;
  margin-bottom: 14px;
}

.bill-meta-grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 18px;
  margin-bottom: 20px;
}

.bill-meta-grid h3 {
  font-size: 14px;
  font-weight: 600;
  color: #1ba7a5;
}

.bill-meta-grid p {
  font-size: 15px;
  padding-top: 4px;
}

/* TABLE */
.bill-table {
  width: 100%;
  border-collapse: collapse;
  margin-top: 15px;
}

.bill-table th {
  padding: 12px;
  font-size: 14px;
  background: #eef6ff;
  color: #003e8a;
  border-bottom: 2px solid #d4e7ff;
}

.bill-table td {
  padding: 12px;
  border-bottom: 1px solid #e7edf7;
  font-size: 15px;
}

.bill-table tr:hover td {
  background: #f5fbff;
}

/* TOTAL BOX */
.bill-total-box {
  margin-top: 30px;
  border-radius: 12px;
  background: #ffffff;
  padding: 20px;
  border: 1px solid #d0ecf0;
}

.bill-total-line {
  display: flex;
  justify-content: space-between;
  padding: 10px 0;
  color: #003e8a;
}

.bill-total-border {
  border-bottom: 1px solid #dceaf3;
}

.bill-final {
  display: flex;
  justify-content: space-between;
  margin-top: 20px;
  padding: 14px 18px;
  border-radius: 12px;
  background: rgba(27, 167, 165, 0.1);
  border: 1px solid #1ba7a5;
  font-size: 20px;
  color: #003e8a;
  font-weight: 800;
}
